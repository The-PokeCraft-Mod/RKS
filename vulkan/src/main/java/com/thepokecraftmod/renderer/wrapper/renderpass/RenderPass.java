package com.thepokecraftmod.renderer.wrapper.renderpass;

import com.thepokecraftmod.renderer.impl.GlobalBuffers;
import com.thepokecraftmod.renderer.wrapper.core.Swapchain;

public class RenderPass {

    public RenderPass build(Swapchain swapchain, GlobalBuffers globalBuffers) {
        return null;
    }
}
